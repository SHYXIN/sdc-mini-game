{
  "name": "sdc-mini-game",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/game.js --sourcemap",
    "dev": "esbuild src/main.ts --bundle --outfile=dist/game.js --sourcemap --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.6"
  }
}
